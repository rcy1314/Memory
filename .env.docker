# Docker环境变量配置文件
# 复制此文件并根据实际情况修改配置

# 外部数据库主机地址
# 在本地Docker环境中使用 host.docker.internal
# 在生产环境中使用实际的数据库服务器地址
EXTERNAL_DB_HOST=host.docker.internal

# PostgreSQL 数据库配置示例
# DB_TYPE=postgresql
# DB_HOST=${EXTERNAL_DB_HOST}
# DB_PORT=5432
# DB_NAME=memory_db
# DB_USER=your_username
# DB_PASSWORD=your_password
# DB_SSL=true

# MySQL 数据库配置示例
# DB_TYPE=mysql
# DB_HOST=${EXTERNAL_DB_HOST}
# DB_PORT=3306
# DB_NAME=memory_db
# DB_USER=your_username
# DB_PASSWORD=your_password
# DB_SSL=false

# 连接池配置
# DB_POOL_SIZE=10
# DB_TIMEOUT=30

# 前端环境变量配置
# 在Docker环境中，前端和后端在同一个容器中运行
# 前端通过相对路径访问后端API，无需代理
VITE_PUBLIC_PATH=/
VITE_USE_PROXY=false
VITE_BASE_API=/api/v1

# 域名反代配置
# 当使用域名反代时，确保数据库服务器允许来自以下地址的连接：
# 1. Docker容器的IP地址范围
# 2. 反代服务器的IP地址
# 3. 如果使用云服务，配置相应的安全组规则