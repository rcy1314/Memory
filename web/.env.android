# Android环境配置
# 资源公共路径,需要以 /开头和结尾
VITE_PUBLIC_PATH = '/'

# Android API配置 - 使用远程API地址
# 注意：Android应用无法访问localhost，需要使用实际的服务器地址
# 这里使用相对路径，实际部署时需要配置正确的API服务器地址
VITE_BASE_API = '/api/v1'

# Android不使用代理
VITE_USE_PROXY = false

# 是否启用压缩
VITE_USE_COMPRESS = true

# 压缩类型
VITE_COMPRESS_TYPE = gzip